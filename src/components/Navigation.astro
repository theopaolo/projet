<nav class="main-nav navanimation">
    <ul>
        <li><a href="index.html">Accueil</a></li>
        <li><a href="index.html#massages">Massages</a></li>
        <li><a href="ayurveda.html">L'Ayurveda</a></li>
        <!-- <li><a href="ateliers.html">Ateliers</a></li> -->
        <!-- <li><a href="journal.html">Journal</a></li> -->
        <li><a href="index.html#contact">Contacter</a></li>
    </ul>
</nav>

<style>
  nav a {
    color: var(--primary-color);
    text-decoration: none;
  }
  nav {
    background-color: transparent; /* Make the background transparent */
    padding: 0.5rem 1rem;

    @screen md {
      padding: 0.5rem 2rem;
    }

    width: fit-content;
    border-radius: 9999px;
    position: fixed;
    bottom: 2rem;
    margin-left: 50%;
    transform: translateX(-50%);
    opacity: 1;
    overflow: hidden;
    border: 1px solid rgba(220, 205, 195, 0.8);
    z-index: 2; /* Ensure nav is above the pseudo-element */
  }
  .main-nav::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(200, 190, 210, 0.6); /* Darker background */
    border-radius: 9999px;
    backdrop-filter: blur(10px); /* Adjust blur */
    z-index: -1; /* Place behind the content */
  }

  nav ul {
    position: relative;
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    font-size: 1rem;
    z-index: 3; /* Ensure list items are above the pseudo-element */
  }

  nav a {
    text-decoration: none;
  }

  @keyframes navanimation {
    0%,
    50% {
      transform: translate(-50%, 100%);
      opacity: 0;
    }
    50%,
    80% {
      opacity: 1;
      transform: translate(-50%, 1rem);
    }
    80%,
    100% {
      opacity: 1;
      transform: translate(-50%, 1rem);
    }
  }

  .navanimation {
    opacity: 0;
    animation: navanimation 1.5s cubic-bezier(0.455, 0.03, 0.515, 0.955) 1.5s forwards;
  }
</style>